apiVersion: batch/v1
kind: Job
metadata:
  name: merge-reportsrun-1746118336
  namespace: playwright
  annotations: # This should help recreate the job
    "argocd.argoproj.io/hook": PreSync
    "argocd.argoproj.io/hook-delete-policy": HookSucceeded

spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: merge
        image: mcr.microsoft.com/playwright:v1.52.0-noble
        command: ["/bin/sh", "-c"]
        args:
          - |
            #npx playwright merge-reports /results/*/*/test-results.json --reporter html;
            npx playwright merge-reports /results/${RUN_ID}/*/report.json --reporter html
            cp -r playwright-report /results/${RUN_ID}/final
            sleep 30000
        env:
        - name: RUN_ID
          value: run-1746118336
        volumeMounts:
        - name: results
          mountPath: /results
      volumes:
      - name: results
        persistentVolumeClaim:
          claimName: results-pvc
